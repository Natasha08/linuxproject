//If user input is == to [food_name or data.food_name or data[i].food_name],
//then display the the row. Search should begin to guess from the first letter.
var mysql = require('mysql');
var mysql = require('./require');
//var output = document.getElementById("response");

module.exports = function (userName, userPass){
	return()

var efridge;

connection.query('select * from  efridge', function (err, rows) {

	efridge = rows;

});

}
module.exports = efridge;